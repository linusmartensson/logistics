<div class="hero-unit">
  <h2 class="id"><%= params.id %></h2>
  <%- linkTo('Edit this transaction', editTransactionPath(params.id), {class: 'btn pull-right'}); %>
  <%- linkTo('Back to list', transactionPath(''), {class: 'btn pull-right'}); %>
</div>

<div id="transactions-list">
<% if (transactions) { %>
<%- buildTable([{text:"Vara", field:"ware.name"},
                {text:"Transaktionstid", field:"createdAt"},
		{text:"Antal", field:"count"},
                {text:"Plats", field:"place.name"}], transactions, transactionPath, "id"); %>
<% } %>
</div>
