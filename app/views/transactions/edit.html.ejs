<div class="hero-unit">
  <form id="transaction-form" class="form-horizontal" action="/transactions/<%= params.id %>?_method=PUT" method="POST">
    <fieldset>
      <legend>Update this Transaction</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {transaction: transaction, wares:wares, places:places}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Save', {tabindex:1, type: 'submit', class: 'btn btn-primary'}) %>
        <%- contentTag('button', 'Remove', {tabindex:1, type: 'submit', formaction: '/transactions/' + params.id + '?_method=DELETE', formmethod: 'POST', class: 'btn btn-danger'}) %>
      </div>
    </fieldset>
  </form>
</div>
