<div class="hero-unit">
  <form id="transaction-form" class="form-horizontal" action="/transactions" method="POST">
    <fieldset>
      <legend>Create a new Transaction</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {transaction: params, wares: wares, places: places}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {tabindex:1, type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>
