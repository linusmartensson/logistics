<div class="hero-unit">
  <form id="order-form" class="form-horizontal" action="/orders" method="POST">
    <fieldset>
      <legend>Create a new Order</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {order: params, wares: wares, places: places}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {tabindex:1, type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>
