<%= partial('layout_header'); %>
  <body>
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="i-bar"><i class="icon-chevron-down icon-white"></i></span>
          </a>
          <%- linkTo('Skafferiet', mainsPath, {class: 'brand'}) %>
          <div class="nav-collapse">
            <ul class="nav">
              <li <%=is_current_page(mainsPath)?"class=\"active\"":""%>><%- linkTo('Hem', mainsPath) %></li>
              <li <%=is_current_page(ordersPath)?"class=\"active\"":""%>><%- linkTo('Beställningar', ordersPath) %></li>
              <li <%=is_current_page(waresPath)?"class=\"active\"":""%>><%- linkTo('Varor', waresPath) %></li>
              <li <%=is_current_page(placesPath)?"class=\"active\"":""%>><%- linkTo('Platser', placesPath) %></li>
              <li <%=is_current_page(transactionsPath)?"class=\"active\"":""%>><%- linkTo('Transaktioner', transactionsPath) %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>
    <div class="container">
      <%- displayFlash(flash); %>
      <%- render(); %>
    </div>
  </body>
<%= partial('layout_footer'); %>

